/* eslint-disable camelcase */
export default interface IWorkExperience {
  id: number;
  location: string;
  company: string;
  start_date: string;
  end_date: string;
  current: boolean;
  role: string;
}
